# arc's own integration gates — the tool dogfoods itself.
[gates.build]
command = "cargo build --all-targets"

[gates.test]
command = "cargo test"

[gates.lint]
command = "cargo clippy --all-targets -- -D warnings && cargo fmt --check"
